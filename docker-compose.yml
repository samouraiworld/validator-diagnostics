services:
  clamav:
    image: clamav/clamav:stable
    volumes:
      - clamav-data:/var/lib/clamav
      # Raises clamd's stock stream limit to the 2 GiB the portal accepts,
      # and makes it report content it skipped for exceeding a limit
      # instead of answering OK — see the file's own header. Note this
      # replaces the image's config wholesale, so anything the stock file
      # set is gone unless clamd.conf sets it too.
      - ./clamd.conf:/etc/clamav/clamd.conf:ro
      # clamd spools each INSTREAM body here (TemporaryDirectory in
      # clamd.conf). On the container's writable layer, concurrent 2 GiB
      # uploads would eat the host's Docker storage instead.
      - clamav-tmp:/var/tmp/clamav
    # clamd holds the whole signature database resident (~1-2 GB). Without
    # a ceiling a spike takes the host down with it; with one, the
    # container is the only casualty and the portal 503s until it is back.
    mem_limit: 4g
    healthcheck:
      test: ["CMD", "clamdcheck.sh"]
      interval: 30s
      timeout: 10s
      # Budget generously: the portal has `condition: service_healthy`, so
      # exhausting these retries doesn't just delay startup, it fails the
      # portal container outright. A first-boot CVD download on a slow
      # link is the case that overruns.
      retries: 20
      start_period: 300s # first boot downloads the virus database

  minio:
    image: minio/minio:latest
    command: server /data --address ":9001" --console-address ":9002"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio-data:/data

  minio-init:
    image: minio/mc:latest
    depends_on:
      - minio
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      S3_BUCKET: ${S3_BUCKET:-submissions}
    entrypoint: >
      sh -c "
      until mc alias set local http://minio:9001 \"$$MINIO_ROOT_USER\" \"$$MINIO_ROOT_PASSWORD\"; do sleep 1; done &&
      mc mb --ignore-existing local/$$S3_BUCKET
      "

  portal:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      minio-init:
        condition: service_completed_successfully
      clamav:
        condition: service_healthy
    environment:
      ADMIN_OPERATOR_ADDRESSES: ${ADMIN_OPERATOR_ADDRESSES}
      ADMIN_SESSION_SECRET: ${ADMIN_SESSION_SECRET:-}
      SESSION_SECRET: ${SESSION_SECRET:-}
      S3_ACCESS_KEY: ${MINIO_ROOT_USER}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
    command:
      - -remote=${REMOTE}
      - -addr=0.0.0.0:8888
      - -s3-bucket=${S3_BUCKET:-submissions}
      - -s3-region=us-east-1
      - -s3-endpoint=http://minio:9001
      - -log-path=/data/submissions.jsonl
      - -exercise-path=/data/exercise.json
      - -scores-path=/data/scores.json
      - -clamav-addr=clamav:3310
      - -clamav-timeout=15m
      # Must stay <= clamd.conf's StreamMaxLength.
      - -max-upload-size=${MAX_UPLOAD_SIZE:-2147483647}
      # Streamed, not buffered — see submission.ValidateArchive and
      # submission.OpenLog. The ceiling here is decompression work, not
      # resident memory.
      - -max-log-size=${MAX_LOG_SIZE:-268435456}
    volumes:
      - portal-logs:/data
    ports:
      - "${PORTAL_PORT:-8080}:8888"

volumes:
  minio-data:
  portal-logs:
  clamav-data:
  clamav-tmp:
