# clamd configuration for the docker-compose stack, bind-mounted over
# /etc/clamav/clamd.conf in the clamav/clamav:stable image.
#
# It exists for one reason: clamd's stock limits are far below the size
# of archive this portal accepts. With stock config clamd rejects
# essentially every real submission with "INSTREAM size limit exceeded."
# and the portal — correctly failing closed — answers 503.
#
# 2 GiB is the number this deployment standardises on: it is the same
# bound as cmd/portal's -max-upload-size default, comfortably above any
# realistic validator archive, and at ClamAV's own documented ceiling for
# these directives. Change one of the two and you must change the other,
# or uploads clamd cannot scan will start being rejected.
DatabaseDirectory /var/lib/clamav
LogTime yes
Foreground yes

TCPSocket 3310
LocalSocket /tmp/clamd.sock

StreamMaxLength 2G
MaxFileSize 2G
MaxScanSize 2G

# The setting that keeps the AV layer honest. MaxScanSize bounds
# *recursively extracted* bytes, and a submission nests two deep
# (archive.tar.gz -> tar -> gnoland.log.gz -> plaintext), so the limits
# above are reachable — cmd/portal's -max-log-size alone permits on the
# order of a gigabyte of decompressed log.
#
# clamd's default here is "no", which means content skipped for exceeding
# a limit is simply not scanned and the stream still answers "stream: OK".
# The portal would then store an unscanned archive as clean: a silent
# fail-open underneath a layer whose whole design is to fail closed.
# With this on, clamd instead reports Heuristics.Limits.Exceeded.*, which
# clamav/clamd.go maps to a scan error (503), not a malware verdict.
AlertExceedsMax yes

# Reloading the signature database builds a second engine in memory
# before swapping, which roughly doubles clamd's footprint mid-exercise.
# The stock image leaves this on; this deployment trades a brief pause
# during reloads for a predictable memory ceiling, because the failure
# mode of the alternative is an OOM kill that 503s every upload.
ConcurrentDatabaseReload no

# clamd buffers each INSTREAM body to disk. Without this it lands on the
# container's writable layer, where concurrent 2 GiB uploads can fill the
# host's Docker storage; docker-compose.yml backs this path with a volume.
TemporaryDirectory /var/tmp/clamav

# Streaming 2 GiB over a slow uplink means long gaps are normal; the
# stock 30s read timeout would hang up on a legitimate upload.
ReadTimeout 600
